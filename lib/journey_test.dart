library journey_test;

import 'package:flutter/material.dart';

class JouneyTest extends StatelessWidget {
  const JouneyTest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text('Journey'),
        ),
        body: const Center(
          child: Text('Journey'),
        ));
  }
}
